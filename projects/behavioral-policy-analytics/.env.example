# Optional Reddit API keys (only needed if using --sentiment-source reddit)
REDDIT_CLIENT_ID=your_client_id
REDDIT_CLIENT_SECRET=your_client_secret
REDDIT_USER_AGENT=sentiment-research-app/0.1 by your_username

# Optional X/Twitter API key (not used in default workflow)
X_BEARER_TOKEN=your_bearer_token
